name: Check Reminders
on:
  schedule:
    - cron: "*/15 * * * *"   # every 15 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call check-reminders endpoint
        run: curl -X POST https://web-production-3f04d.up.railway.app/check-reminders
